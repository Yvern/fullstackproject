import React from 'react'

const Dashboard = () => (
    <div className="content-container">
        <p className="App-intro">
            To get started, edit <code>src/App.js</code> and save to reload. Add lots of information to try
            and overflow the content area so I can see what happens and change the css accordingly.
        </p>
    </div>
)

export default Dashboard